# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
/build
*.zip

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.history

# secrets
secrets.*.js

# dev harness: throwaway chrome profile, its wallet and its screenshots
/.dev-harness

# the SDK's StorageController writes this when it is required outside a browser,
# which the checks in utils/ do
/znn-local-storage

# Windows Explorer folder settings
desktop.ini

.env
AGENTS.md
.codex
.plans
